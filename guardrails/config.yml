models:
  - type: main
    engine: google
    model: gemini-2.5-flash

rails:
  input:
    flows:
      - check_dangerous_content
      - check_code_content
      - check_unrelated_content
  dialog:
    flows:
      - greeting